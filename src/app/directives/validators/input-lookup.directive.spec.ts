import { InputLookupDirective } from './input-lookup.directive';

describe('InputLookupDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLookupDirective();
    expect(directive).toBeTruthy();
  });
});
